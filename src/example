//%precondition b % 4 == 2
//%postcondition c - 2 == 1
fn a (b: i32, c:bool, a: i32 ) -> bool {
let mut a: i32 = 14;

// just ignore me

// the one below is not to be ignored
//the one below is not to be ignored
//%assert a == 14
//%assume b %2 == 1

a = xd(a, lc);
/*
this
also
does not
matter
at all */let b: bool;
// this is just a normal comment
//%loop_invariant c== 3
if a == 14 {
   let mut c = 3;
} else if a == 13 {
   let c = a + 43;
} else {
   let c = a + 123;
}
}